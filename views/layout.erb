<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Госпитализация ДАР</title>
  
  <!-- Favicon (основной) -->
  <link rel="icon" type="image/png" href="/images/favicon.png">

  <!-- Bootstrap 5 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Flatpickr -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">

  <!-- Кастомные стили -->
  <link rel="stylesheet" href="/css/application.css">

<link href="https://fonts.googleapis.com/css2?family=Jost:wght@600;700&display=swap" rel="stylesheet">
<style>
  .logo-link { font-family: 'Jost', sans-serif; font-weight: 600; }
</style>

</head>

<body class="bg-light">
  <!-- Навбар -->
  <nav class="navbar navbar-expand-lg navbar-dark navbar-custom mb-4">
    <div class="container">
      <a class="navbar-brand fw-bold d-flex align-items-center logo-link" href="/">
        <img src="/images/logo.png" alt="Логотип" class="logo-img">
        Госпитализация ДАР
      </a>
      <div class="d-flex align-items-center">
        <a href="/reports" class="text-white ms-2 report-icon" title="Отчеты">
          <i class="bi bi-file-earmark-text fs-2"></i>
        </a>
      </div>
    </div>
  </nav>

  <!-- Основной контент -->
  <div class="container">
    <%= yield %>
  </div>

  <!-- Подвал -->
  <footer class="mt-5 py-3 text-center text-muted">
    <div class="container">
      <small>© <%= Time.now.year %> Госпитализация ДАР. Все права защищены.</small>
    </div>
  </footer>

  <!-- Подключение скриптов -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
  <script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/ru.js"></script>
  <script src="/js/application.js"></script>

<!-- Кастомное модальное окно -->
<div class="modal fade" id="confirmModal" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content border-0 shadow-lg">
      <div class="modal-header border-0" style="background-color: var(--main-color);">
        <h5 class="modal-title text-white"><i class="bi bi-question-circle me-2"></i>Подтверждение</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body text-center py-4">
        <div class="icon-wrapper mb-3">
          <i class="bi bi-exclamation-triangle-fill text-warning" style="font-size: 3rem;"></i>
        </div>
        <h4 id="confirmMessage" class="mb-3">Вы действительно хотите освободить койку?</h4>
      </div>
      <div class="modal-footer border-0 justify-content-center">
        <button type="button" class="btn btn-lg btn-outline-secondary px-4" id="cancelBtn">
          <i class="bi bi-x-circle me-2"></i>Отмена
        </button>
        <button type="button" class="btn btn-lg btn-danger px-4" id="confirmBtn" style="background-color: var(--main-color-dark); border-color: var(--main-color-dark);">
          <i class="bi bi-check-circle me-2"></i>Освободить
        </button>
      </div>
    </div>
  </div>
</div>

</body>
</html>